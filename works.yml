version: 1

work:
  verify:
    triggers:
      - pull_request
    requirements:
      confidence: development
      os: linux
      arch: amd64
      pool: avc-core
    nodes:
      activate-v21-production:
        run: >-
          set -euo pipefail;
          chmod +x scripts/ops/v21-production-activate.sh;
          EXPECTED_VDS_HOST=vmi3517816 scripts/ops/v21-production-activate.sh
        timeout_s: 900
