version: 1
work:
  verify:
    triggers: [pull_request]
    requirements:
      confidence: development
      os: linux
      arch: amd64
      pool: avc-core
    nodes:
      tg-process-git-metadata:
        run: >-
          set -eu;
          test "$(hostname -s)" = "vmi3517816";
          if [ "$(id -u)" -eq 0 ]; then P=""; else sudo -n true; P="sudo -n"; fi;
          PID="$($P systemctl show tg-gateway.service -p MainPID --value)";
          $P test -e "/proc/$PID/cwd/.git";
          echo "tg-process-git-metadata=PASS"
        timeout_s: 30
