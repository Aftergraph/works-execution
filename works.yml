# Temporary one-shot bootstrap pipeline.
# Purpose: repair the production WORKS binaries from exact current main.
# This branch is never merged; reset it to main after the receipt is VERIFIED.
version: 1

work:
  bootstrap:
    triggers:
      - push
    requirements:
      confidence: development
      os: linux
      arch: amd64
      pool: avc-core
    nodes:
      bootstrap-integrity-live:
        run: bash scripts/ops/bootstrap-integrity-live.sh
        timeout_s: 60
