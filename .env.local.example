# Local dev secrets for M1. NOT committed to git (see .gitignore).
# Regenerate before sharing or pushing.

# GitHub PAT for JonasAbde — full repo scope, can create Check Runs.
WORKS_GITHUB_TOKEN=

# Shared webhook secret. GitHub signs each delivery with this.
# Generate fresh with: openssl rand -hex 32
WORKS_GITHUB_WEBHOOK_SECRET=

# Smee.io channel that forwards GitHub → our local webhook.
WORKS_GITHUB_WEBHOOK_PROXY=https://smee.io/Yn21lr2MAMDbeLr7
